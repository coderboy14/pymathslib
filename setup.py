from setuptools import find_packages, setup

setup(
    name="mathslib",
    packages=find_packages(),
    version="1.0.0",
    description="Math library",
    author="Ethan Manzi",
    license="MIT",
    install_requires=[],
    setup_requires=[""],
    tests_require=[""],
    # test_suite="tests",
)